# YARA vs CEL：极致技术对比调研

> "Simplicity is the ultimate sophistication." — Leonardo da Vinci
>
> "When something is important enough, you do it even if the odds are not in your favor." — Elon Musk

---

## 执行摘要

本报告以**乔布斯对产品的极致追求**和**马斯克对技术的第一性原理**为双重标准，对 YARA 规则引擎与 CEL（Common Expression Language）进行全面深度对比。

**核心结论**：YARA 和 CEL 不是竞争关系，而是**互补关系**。它们解决的是完全不同层次的问题：

| 维度 | YARA | CEL |
|------|------|-----|
| 核心问题 | 在字节流中搜索模式 | 评估结构化数据上的表达式 |
| 类比 | **搜索引擎** | **计算引擎** |
| 问题类型 | "这是什么？" | "该怎么办？" |
| 极致追求 | 扫描速度 | 评估安全性 |

---

## 一、架构哲学层面

### 1.1 YARA：模式匹配的极致

**设计哲学**：专注做一件事——在海量数据中快速找到特定模式。

```
┌─────────────────────────────────────────────────────────────┐
│                    YARA 架构                                │
├─────────────────────────────────────────────────────────────┤
│  规则编译 → 原子提取 → Aho-Corasick自动机 → 字节码验证 → 条件评估 │
│     ↓           ↓              ↓                ↓            │
│   解析DSL    提取4字节      O(n)线性扫描      正则验证         │
│             最佳子串                                         │
└─────────────────────────────────────────────────────────────┘
```

**核心算法**：Aho-Corasick 自动机
- 时间复杂度：O(n + m + z)
  - n = 输入数据长度
  - m = 所有模式总长度
  - z = 匹配数量
- 空间换时间：构建有限状态自动机，一次扫描匹配所有模式

**乔布斯视角**：
- ✅ "做一件事，做到极致"
- ✅ 语法直观：`strings + condition` 结构清晰
- ⚠️ 但正则表达式和十六进制模式有学习曲线

**马斯克视角（第一性原理）**：
- 前提：恶意软件有可识别的特征
- 推论：特征可以用模式描述
- 结论：快速扫描模式 = 快速检测恶意软件
- 设计取舍：**牺牲通用性，换取扫描速度**

### 1.2 CEL：安全表达式的极致

**设计哲学**：让用户安全地定义和执行表达式，永不失控。

```
┌─────────────────────────────────────────────────────────────┐
│                    CEL 架构                                  │
├─────────────────────────────────────────────────────────────┤
│  表达式 → 词法分析 → 语法分析 → 类型检查 → AST优化 → 求值     │
│    ↓         ↓          ↓          ↓         ↓        ↓     │
│  CEL语法  Token流    AST树    渐进类型   常量折叠  纳秒级执行  │
└─────────────────────────────────────────────────────────────┘
```

**核心特性**：非图灵完备（Non-Turing Complete）
- 无循环
- 无递归
- 表达式**总是终止**
- 可静态分析最大执行步骤

**乔布斯视角**：
- ✅ 极简语法：类似单行 Lambda
- ✅ C-like 语法，对开发者零学习成本
- ✅ "最好的安全是你感觉不到的安全"

**马斯克视角（第一性原理）**：
- 前提：策略需要用户定义
- 问题：用户代码可能有安全风险
- 解决：去掉图灵完备性，保留必要表达力
- 设计取舍：**牺牲表达力，换取安全性和可预测性**

### 1.3 架构哲学对比总结

| 维度 | YARA | CEL | 极致标准评价 |
|------|------|-----|-------------|
| 设计目标 | 模式搜索 | 表达式求值 | 各有所长 |
| 核心算法 | Aho-Corasick | AST 解释器 | YARA 更底层 |
| 图灵完备 | 否（规则层面） | 否（刻意设计） | CEL 更彻底 |
| 简洁性 | 中（DSL 有门槛） | 高（类 C 语法） | **CEL 胜** |
| 第一性原理 | 扫描 = 状态机 | 安全 = 限制能力 | 各有洞见 |

---

## 二、性能表现层面

### 2.1 YARA 性能特征

**扫描性能基准**（基于 Neo23x0/panopticon 测试框架）：

| 场景 | 性能指标 | 备注 |
|------|---------|------|
| 单文件扫描（1MB） | 1-10 ms | 取决于规则复杂度 |
| 批量扫描（100 文件） | 100-500 ms | 近似线性扩展 |
| 大规则集（10,000 规则） | 扫描速度下降 20-50% | 自动机构建开销 |
| 内存占用 | ~100 MB / 80,000 规则 | 编译后规则 ~60 MB |

**性能瓶颈分析**：

```
性能热点分布：
┌─────────────────────────────────────────┐
│ Aho-Corasick 扫描      │████████████ 60% │
│ 正则表达式验证         │█████ 25%        │
│ 条件评估               │███ 15%          │
└─────────────────────────────────────────┘
```

**原子（Atoms）选择的影响**：
```yara
// 差的规则：原子太短，匹配太多
rule bad_rule {
    strings:
        $a = { 00 00 }  // 2字节，极其常见
    condition:
        $a
}

// 好的规则：原子足够长且独特
rule good_rule {
    strings:
        $a = { 4D 5A 90 00 03 00 00 00 }  // PE 头，8字节
    condition:
        $a at 0
}
```

### 2.2 CEL 性能特征

**表达式求值基准**（Google 官方数据）：

| 表达式类型 | 求值时间 | 备注 |
|-----------|---------|------|
| 简单布尔 | 10-50 ns | `a > b` |
| 字符串比较 | 50-100 ns | `s.startsWith("prefix")` |
| 复杂表达式 | 100-500 ns | 多条件组合 |
| 函数调用 | 1-10 μs | 取决于函数复杂度 |
| 列表操作 | 1-100 μs | `list.all(x, x > 0)` |

**编译 vs 求值分离**：
```
┌──────────────────────────────────────────────────────────┐
│ 编译阶段（一次性开销）                                    │
│ • 词法分析：~10 μs                                       │
│ • 语法分析：~20 μs                                       │
│ • 类型检查：~30 μs                                       │
│ • AST 优化：~10 μs                                       │
│ 总计：~70 μs（可忽略，只执行一次）                        │
├──────────────────────────────────────────────────────────┤
│ 求值阶段（热路径）                                        │
│ • 简单表达式：10-100 ns                                  │
│ • 复杂表达式：100 ns - 10 μs                            │
│ • 零内存分配（预分配求值上下文）                          │
└──────────────────────────────────────────────────────────┘
```

### 2.3 性能对比分析

| 指标 | YARA | CEL | 差异原因 |
|------|------|-----|---------|
| **典型延迟** | 1-100 ms | 10 ns - 10 μs | YARA 扫描字节流，CEL 处理结构化数据 |
| **延迟确定性** | 低 | **高** | CEL 可预测，YARA 依赖输入大小 |
| **吞吐量** | 10-100 MB/s | **100,000+ 次/s** | 不同度量单位 |
| **内存模式** | 持续增长 | **稳定** | YARA 规则累积，CEL 编译后固定 |
| **冷启动** | 100-500 ms | 70 μs | YARA 需构建自动机 |

**为什么不能简单比较？**

```
YARA 工作模式：                    CEL 工作模式：
                                   
文件(1MB) ──→ [扫描] ──→ 结果     结构化数据 ──→ [求值] ──→ 结果
     ↑                                  ↑
   原始字节                          已解析
   需要遍历                          直接访问
```

**极致评价**：
- **YARA 的"慢"是必要的代价**：扫描原始字节流本质上需要 O(n) 时间
- **CEL 的"快"是设计的胜利**：输入已结构化，只需 O(1) 或 O(log n) 访问

---

## 三、安全性层面

### 3.1 YARA 安全特性

**优点**：
| 特性 | 说明 |
|------|------|
| 声明式 | 规则只描述"是什么"，不执行代码 |
| 无副作用 | 不修改文件、不访问网络 |
| 隔离性 | 规则之间相互独立 |

**风险点**：

**1. ReDoS（正则表达式拒绝服务）**
```yara
// 危险：可能导致指数级回溯
rule redos_risk {
    strings:
        $a = /a{1,100}b{1,100}c{1,100}/  // 对某些输入极慢
    condition:
        $a
}
```

**2. 资源耗尽**
```
风险场景：
• 大量规则（10万+）→ 内存耗尽
• 复杂正则 → CPU 饱和
• 无内置资源限制机制
```

**3. 无沙箱隔离**
- YARA 运行在宿主进程中
- 恶意规则可通过资源耗尽影响宿主
- 需要外部沙箱（如容器）隔离

### 3.2 CEL 安全特性

**Security by Design（设计即安全）**：

| 安全机制 | 实现方式 | 效果 |
|---------|---------|------|
| **非图灵完备** | 无循环、无递归 | 表达式总是终止 |
| **类型安全** | 渐进类型系统 | 编译时捕获类型错误 |
| **沙箱执行** | 无 I/O、无副作用 | 不能访问系统资源 |
| **资源边界** | 可配置执行步数限制 | 防止长时间运行 |
| **确定性** | 相同输入 = 相同输出 | 可审计、可复现 |

**CEL 安全保证代码示例**：
```go
// 配置 CEL 执行环境
env, _ := cel.NewEnv(
    cel.Declarations(
        decls.NewVar("request", decls.NewMapType(decls.String, decls.Dyn)),
    ),
    // 限制执行步数，防止复杂表达式耗尽资源
    cel.EvalOptions(cel.OptTrackCost),
)

// 编译表达式（类型检查在这里完成）
ast, issues := env.Compile(`request.user.role == "admin"`)
if issues.Err() != nil {
    // 类型错误在编译时就能发现
    log.Fatal(issues.Err())
}

// 求值（在沙箱中执行，无法突破）
program, _ := env.Program(ast)
result, _, _ := program.Eval(map[string]interface{}{
    "request": requestData,
})
```

### 3.3 安全性对比

| 安全维度 | YARA | CEL | 评价 |
|---------|------|-----|------|
| **代码注入风险** | 低（声明式） | **极低**（无执行能力） | CEL 胜 |
| **DoS 防护** | 中（ReDoS 风险） | **高**（总是终止） | CEL 胜 |
| **沙箱隔离** | 无（需外部） | **内置** | CEL 胜 |
| **类型安全** | 弱 | **强**（渐进类型） | CEL 胜 |
| **资源限制** | 无内置 | **可配置** | CEL 胜 |
| **审计能力** | 中 | **高**（确定性执行） | CEL 胜 |

**乔布斯视角**：
> CEL 把安全做成了"默认行为"，用户不需要思考安全问题

**马斯克视角**：
> 去掉图灵完备性是"减法创新"——减掉一个特性，获得整个安全保证

---

## 四、可扩展性层面

### 4.1 规则/表达式数量扩展

**YARA**：
```
规则数量 vs 性能：
┌────────────────────────────────────────────────────────────┐
│ 规则数   │ 编译时间  │ 扫描速度  │ 内存占用                │
├──────────┼───────────┼───────────┼────────────────────────┤
│ 1,000    │ ~100 ms   │ 100%      │ ~10 MB                 │
│ 10,000   │ ~1 s      │ ~80%      │ ~100 MB                │
│ 100,000  │ ~10 s     │ ~50%      │ ~1 GB                  │
└────────────────────────────────────────────────────────────┘
```

**CEL**：
```
表达式数量 vs 性能：
┌────────────────────────────────────────────────────────────┐
│ 表达式数 │ 总编译时间 │ 单次求值  │ 内存占用              │
├──────────┼────────────┼───────────┼───────────────────────┤
│ 100      │ ~7 ms      │ ~100 ns   │ ~1 MB                 │
│ 1,000    │ ~70 ms     │ ~100 ns   │ ~10 MB                │
│ 10,000   │ ~700 ms    │ ~100 ns   │ ~100 MB               │
└────────────────────────────────────────────────────────────┘
注：单次求值时间不随表达式总数增加
```

### 4.2 并发处理能力

**YARA**：
- 规则：编译后可多线程共享（只读）
- 扫描：每个文件可独立并行处理
- 瓶颈：内存带宽（大规则集时）

**CEL**：
- 表达式：编译后不可变，天然线程安全
- 求值：完全无状态，完美并行
- 瓶颈：几乎没有（CPU 密集型操作极少）

```go
// CEL 并发求值示例
func evaluateConcurrently(program cel.Program, events []Event) []bool {
    results := make([]bool, len(events))
    var wg sync.WaitGroup
    
    for i, event := range events {
        wg.Add(1)
        go func(idx int, e Event) {
            defer wg.Done()
            result, _, _ := program.Eval(e.ToMap())  // 完全无锁
            results[idx] = result.Value().(bool)
        }(i, event)
    }
    
    wg.Wait()
    return results
}
```

### 4.3 企业级部署扩展性

| 维度 | YARA | CEL |
|------|------|-----|
| **规则分发** | 文件分发 | 字符串/字节分发 |
| **热更新** | 需重新编译所有规则 | 单个表达式独立更新 |
| **版本管理** | 规则文件版本 | 表达式粒度版本 |
| **A/B 测试** | 困难 | 容易（表达式级别） |
| **回滚能力** | 整体回滚 | 精确回滚 |

---

## 五、开发体验层面

### 5.1 语法简洁性对比

**YARA 规则示例**：
```yara
import "pe"

rule Suspicious_PE_File
{
    meta:
        description = "检测可疑 PE 文件"
        author = "Security Team"
        
    strings:
        $mz = { 4D 5A }                          // MZ 头
        $api1 = "VirtualAlloc" ascii wide nocase
        $api2 = "WriteProcessMemory" ascii wide
        $suspicious = /eval\s*\(/i
        
    condition:
        $mz at 0 and
        filesize < 1MB and
        2 of ($api*) and
        $suspicious
}
```

**等价 CEL 表达式**（用于决策）：
```cel
// 假设 YARA 扫描结果已转换为结构化事件
event.yara_matches.exists(m, m.rule == "Suspicious_PE_File") &&
event.file.size < 1048576 &&
event.file.signature.status != "SIGNED"
```

**语法复杂度对比**：

| 维度 | YARA | CEL |
|------|------|-----|
| 学习曲线 | 中等（需学习十六进制、正则） | **低**（类 C 语法） |
| 表达力 | 高（模式匹配专用） | 中高（通用表达式） |
| 可读性 | 中（专业领域） | **高**（接近自然语言） |
| 错误提示 | 中 | **优秀**（类型系统辅助） |

### 5.2 调试便利性

**YARA**：
```bash
# 调试模式扫描
yara -s -p 4 rules.yar target_file

# 输出匹配的字符串
Suspicious_PE_File target_file
0x0:$mz: 4D 5A
0x1234:$api1: VirtualAlloc
```

**CEL**：
```go
// 详细求值跟踪
details, _ := program.Eval(input, cel.EvalOptions(cel.OptTrackState))

// 可以看到每个子表达式的求值结果
for _, detail := range details {
    fmt.Printf("表达式: %s, 结果: %v\n", detail.Expr, detail.Value)
}
```

### 5.3 IDE 支持

| 工具 | YARA | CEL |
|------|------|-----|
| 语法高亮 | VS Code 插件 | VS Code 插件 |
| 自动补全 | 有限 | **完整**（基于类型） |
| 实时错误检查 | 基础 | **完整**（类型检查） |
| 重构支持 | 无 | **有**（重命名等） |

---

## 六、跨平台兼容性

### 6.1 平台支持

| 平台 | YARA | CEL |
|------|------|-----|
| **Linux** | ✅ 原生支持 | ✅ Go/C++/Java |
| **macOS** | ✅ 原生支持 | ✅ Go/C++/Java |
| **Windows** | ✅ 原生支持 | ✅ Go/C++/Java |
| **FreeBSD** | ✅ | ✅ |
| **WASM** | ❌ | ✅（cel-go 可编译） |

### 6.2 语言绑定

| 语言 | YARA | CEL |
|------|------|-----|
| **C/C++** | ✅ 原生 | ✅ google/cel-cpp |
| **Python** | ✅ yara-python | ⚠️ 有限支持 |
| **Go** | ⚠️ go-yara | ✅ google/cel-go |
| **Java** | ⚠️ 第三方 | ✅ google/cel-java |
| **Rust** | ✅ yara-rust | ⚠️ 第三方 |

### 6.3 行为一致性

**YARA**：
- 各平台行为高度一致（核心算法相同）
- 某些模块（如 magic）不支持 Windows

**CEL**：
- **符合性测试**：数千个测试用例确保跨实现一致
- Google 官方维护 C++/Go/Java 三个参考实现
- 保证：相同表达式 + 相同输入 = 相同输出

---

## 七、生态集成能力

### 7.1 YARA 生态

```
┌─────────────────────────────────────────────────────────────┐
│                    YARA 生态系统                             │
├─────────────────────────────────────────────────────────────┤
│  规则来源：                                                  │
│  • Valhalla（22,000+ 高质量规则）                           │
│  • YARA-Rules GitHub 社区                                   │
│  • 厂商威胁情报                                              │
├─────────────────────────────────────────────────────────────┤
│  集成工具：                                                  │
│  • ClamAV（防病毒引擎）                                      │
│  • Cuckoo Sandbox（动态分析）                               │
│  • THOR/Loki（取证扫描器）                                   │
│  • VirusTotal（在线扫描）                                    │
│  • osquery（终端查询）                                       │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 CEL 生态

```
┌─────────────────────────────────────────────────────────────┐
│                    CEL 生态系统                              │
├─────────────────────────────────────────────────────────────┤
│  核心使用者：                                                │
│  • Google Cloud IAM（访问控制）                             │
│  • Kubernetes（验证策略）                                   │
│  • Envoy Proxy（流量控制）                                  │
│  • Istio（服务网格策略）                                    │
├─────────────────────────────────────────────────────────────┤
│  策略框架：                                                  │
│  • Cerbos（应用授权）                                       │
│  • Kyverno（Kubernetes 策略）                               │
│  • Google Secure Web Proxy                                  │
└─────────────────────────────────────────────────────────────┘
```

### 7.3 集成难度对比

| 场景 | YARA | CEL |
|------|------|-----|
| 恶意软件扫描 | **原生** | 不适用 |
| 安全策略评估 | 不适用 | **原生** |
| EDR/XDR 集成 | **成熟** | 新兴 |
| 云原生安全 | 有限 | **主流** |
| SIEM 集成 | **广泛** | 增长中 |

---

## 八、未来演进潜力

### 8.1 技术趋势分析

**YARA 发展方向**：
- YARA-X：Rust 重写版本，性能提升
- 更好的正则表达式引擎（避免 ReDoS）
- 云原生部署支持
- 与机器学习模型结合

**CEL 发展方向**：
- 更多语言绑定
- 扩展库生态
- 标准化进程（OpenTelemetry 等采用）
- WebAssembly 支持加强

### 8.2 长期竞争力评估

| 维度 | YARA 前景 | CEL 前景 |
|------|----------|----------|
| 技术成熟度 | 稳定 | 快速成长 |
| 社区活跃度 | 活跃 | 非常活跃 |
| 企业采用 | 广泛 | 快速增长 |
| 云原生适配 | 追赶中 | **领先** |
| 替代品威胁 | 低 | 低 |

### 8.3 演进预测

```
2025-2030 技术演进预测：

YARA：
├── 2025: YARA-X 稳定版发布
├── 2026: 原生云扫描服务普及
├── 2027: ML 辅助规则生成成熟
└── 2030: 仍是恶意软件检测黄金标准

CEL：
├── 2025: Kubernetes 全面采用
├── 2026: 更多云厂商标准化
├── 2027: WASM 运行时广泛支持
└── 2030: 策略即代码事实标准
```

---

## 九、代码示例对比

### 9.1 恶意软件检测场景

**YARA 实现**（检测阶段）：
```yara
rule Ransomware_Generic
{
    meta:
        description = "通用勒索软件检测"
        
    strings:
        $ransom_note1 = "Your files have been encrypted" nocase
        $ransom_note2 = "bitcoin" nocase wide
        $crypto_api1 = "CryptEncrypt"
        $crypto_api2 = "CryptGenKey"
        $file_enum = "FindFirstFile"
        
    condition:
        uint16(0) == 0x5A4D and                  // PE 文件
        1 of ($ransom_note*) and                  // 勒索信息
        2 of ($crypto_api*) and                   // 加密 API
        $file_enum                                // 文件枚举
}
```

**CEL 实现**（决策阶段）：
```cel
// 基于 YARA 扫描结果的决策
let yara_match = event.yara_results.exists(r, r.rule == "Ransomware_Generic");
let high_entropy = event.file.entropy > 7.5;
let recent_file = event.file.modified > now - duration("24h");

yara_match && high_entropy && recent_file ? 
    decision.block("检测到勒索软件特征") :
    decision.allow()
```

### 9.2 数据安全策略场景

**YARA 实现**（检测敏感数据）：
```yara
rule Sensitive_Data_PII
{
    meta:
        description = "检测 PII 数据"
        
    strings:
        // 身份证号（简化正则）
        $id_card = /[1-9]\d{5}(19|20)\d{2}(0[1-9]|1[0-2])(0[1-9]|[12]\d|3[01])\d{3}[\dXx]/
        // 银行卡号
        $bank_card = /[3-6]\d{15,18}/
        // 手机号
        $phone = /1[3-9]\d{9}/
        
    condition:
        any of them
}
```

**CEL 实现**（访问控制决策）：
```cel
// 敏感文件访问控制
event.file.tags.contains("PII") &&
event.actor.user.department != "HR" &&
event.actor.user.role != "admin" ?
    decision.deny("非授权用户禁止访问 PII 数据") :
    decision.audit("PII 数据访问已记录")
```

---

## 十、最终推荐

### 10.1 核心结论

> **YARA 和 CEL 不是"二选一"的关系，而是"最佳拍档"**

```
┌──────────────────────────────────────────────────────────────────┐
│                    终端安全数据流                                │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  原始文件/内存 ──→ [YARA 扫描] ──→ 安全事件 ──→ [CEL 决策] ──→ 响应 │
│        ↑                               ↑                ↑       │
│   字节流数据                      结构化数据        策略评估      │
│   "这是什么？"                                    "该怎么办？"    │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 10.2 场景化推荐

| 使用场景 | 推荐技术 | 理由 |
|---------|---------|------|
| 恶意软件静态扫描 | **YARA** | 无可替代的模式匹配能力 |
| 内存取证分析 | **YARA** | 原生支持内存扫描 |
| 安全策略评估 | **CEL** | 安全、快速、可预测 |
| 访问控制决策 | **CEL** | 类型安全、表达力强 |
| 实时事件过滤 | **CEL** | 纳秒级延迟 |
| 威胁情报匹配 | **YARA** | 丰富的规则生态 |

### 10.3 极致架构推荐

**乔布斯 + 马斯克 标准的最优解**：

```
┌─────────────────────────────────────────────────────────────────┐
│                    极致安全架构                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  感知层（YARA）                                                 │
│  ├── 静态文件扫描                                               │
│  ├── 进程内存扫描                                               │
│  └── 输出：威胁标签、匹配规则、置信度                            │
│                                                                 │
│       │ 转换为 OCSF 标准事件                                    │
│       ▼                                                         │
│                                                                 │
│  决策层（CEL）                                                  │
│  ├── 策略评估（阻止/允许/审计）                                 │
│  ├── 风险评分计算                                               │
│  └── 输出：决策结果、理由、建议动作                              │
│                                                                 │
│       │                                                         │
│       ▼                                                         │
│                                                                 │
│  响应层                                                         │
│  ├── 执行阻止/允许                                              │
│  ├── 记录审计日志                                               │
│  └── 发送告警通知                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 10.4 为什么这是"极致设计"？

**乔布斯会喜欢**：
- 每个组件只做一件事，做到极致
- 用户写策略时不需要关心底层扫描细节
- "It just works"

**马斯克会喜欢**：
- 第一性原理：用最合适的工具解决最合适的问题
- 不过度工程：YARA 做搜索，CEL 做计算
- 效率极致：YARA 优化 I/O，CEL 优化 CPU

---

## 附录 A：性能基准测试方法论

### YARA 测试方法
```bash
# 使用 panopticon 测试
git clone https://github.com/Neo23x0/panopticon
cd panopticon
python panopticon.py --rules your_rules.yar --samples ./samples/
```

### CEL 测试方法
```go
// Go benchmark
func BenchmarkCELEval(b *testing.B) {
    env, _ := cel.NewEnv(...)
    ast, _ := env.Compile(`request.user.role == "admin"`)
    program, _ := env.Program(ast)
    
    b.ResetTimer()
    for i := 0; i < b.N; i++ {
        program.Eval(testInput)
    }
}
```

## 附录 B：参考资源

| 资源 | 链接 |
|------|------|
| YARA 官方文档 | https://yara.readthedocs.io/ |
| YARA 性能指南 | https://github.com/Neo23x0/YARA-Performance-Guidelines |
| CEL 官网 | https://cel.dev |
| CEL-Go 代码库 | https://github.com/google/cel-go |
| CEL 语言规范 | https://github.com/google/cel-spec |
| Valhalla 规则库 | https://valhalla.nextron-systems.com |

---

*文档版本：1.0*
*评估标准：乔布斯的极致简洁 + 马斯克的第一性原理*
*核心结论：YARA + CEL = 感知与决策的完美分工*
